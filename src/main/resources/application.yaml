spring:
  datasource:
    url: jdbc:mysql://localhost:3306/marketplace_db
    username: root
    password: 15647821Aa!*

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  servlet:
    session:
      timeout: 60m
  error:
    include-stacktrace: never